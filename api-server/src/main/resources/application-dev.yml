server:
  port: 8095

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/YJS_Default?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: YJS_Default
    password: ${PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jackson:
    serialization:
      fail-on-empty-beans: false
